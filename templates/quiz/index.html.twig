{% extends 'base.html.twig' %}

{% block title %}Hello QuizController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% for quest in questions %}
    {# {% if totalquest.id == [1] %}  #}
        <h1>{{quest}}</h1>
    {# {% endif %} #}
         {% for repon in reponses %}
            {# {% if %} #}
            <div class="custom-control custom-radio">
                <input type="radio" class="custom-control-input" id="{{repon}}" name="{{repon}}" value="{{repon}}" required />
                <label class="custom-control-label" for="{{repon}}">{{repon}}</label>
            </div>
            {# {% endif %} #}
         {% endfor %}
  </label>
{% endfor %}

{% endblock %}
